//! kb-mcp-tray library: Windows-only API exposed to the main `kb-mcp` crate
//! for shell:startup shortcut install/uninstall.
//!
//! PR-1 skeleton: empty. Filled in PR-3 (`install` module).

#![cfg(target_os = "windows")]
