[workspace]
members = ["kb-mcp", "crates/kb-mcp-tray", "crates/kb-mcp-svc"]
resolver = "2"

# The profile that 'dist' will build with (moved from kb-mcp/Cargo.toml during
# feature-44 PR-1 workspace split; profiles must live at the workspace root).
[profile.dist]
inherits = "release"
lto = "thin"
